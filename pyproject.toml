[project]
name = "btc-lakehouse"
version = "0.1.0"
description = "Bitcoin on-chain analytics pipeline"
requires-python = ">=3.13"
authors = [
    { name = "Jordan Burnichon", email = "burnichonjordan@gmail.com" },
]
dependencies = [
    "boto3>=1.43.17",
    "pandas>=2.0",
    "python-dotenv>=1.0",
    "prefect>=3.7.2",
    "pyarrow>=24.0.0",
    "pyiceberg[glue]>=0.7.0",
    "pyspark>=4.1.2",
    "streamlit>=1.40",
]

[project.scripts]
ingest = "cli.ingest:main"
transform = "cli.transform:main"
sync-tables = "cli.sync_tables:main"

[dependency-groups]
dev = [
    "ruff>=0.15.14",
    "pytest>=9.0.3",
    "black>=26.5.1",
]

[tool.uv]
package = true

[tool.ruff]
line-length = 100
